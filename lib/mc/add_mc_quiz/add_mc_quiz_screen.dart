import 'package:flutter/material.dart';

class AddMCQuizScreen extends StatelessWidget {
  const AddMCQuizScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Add MC Quiz screen'),
      ),
    );
  }
}
