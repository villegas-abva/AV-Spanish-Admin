import 'package:flutter/material.dart';

class AddTrueFalseQuizScreen extends StatelessWidget {
  const AddTrueFalseQuizScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Add True false quiz!'),
      ),
    );
  }
}
